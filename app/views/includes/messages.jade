- if (typeof errors != 'undefined' && errors.length)
  .fade.in.alert.alert-danger
    button.btn.close(type='button', data-dismiss='alert') ×
    ul
      - each error in errors
        li!= error

- else if (typeof warning != 'undefined' && warning.length)
  .fade.in.alert.alert-warning
    button.close(type='button', data-dismiss='alert') ×
    ul
      - each warn in warning
        li!= warn

- else if (typeof info != 'undefined' && info.length)
  .fade.in.alert.alert-info
    button.close(type='button', data-dismiss='alert') ×
    ul
      - each inf in info
        li!= inf

- else if (typeof success != 'undefined' && success.length)
  .fade.in.alert.alert-success
    button.close(type='button', data-dismiss='alert') ×
    ul
      - each succ in success
        li!= succ